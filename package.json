{
  "name": "pico-gltest",
  "version": "0.0.13",
  "description": "A test framework for WebGL 1 and 2 applications",
  "bin": {
    "gltest": "gltest"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tsherif/pico-gltest"
  },
  "scripts": {
    "lint": "eslint gltest page/gltest.js",
    "test": "npm run lint && ./gltest && npm run test-full-config && npm run test-cli-args && npm run test-cli-equal-args",
    "test-full-config": "./gltest --config test/test-configs/full-config.json && test -d full-config-results  && test -d .nyc_output && rimraf full-config-results",
    "test-cli-args": "./gltest --clear-config --output-dir cli-results --server-port 5151 --asset-dir test/assets/ --coverage true, --headless true test/test.js && test -d cli-results  && test -d .nyc_output && rimraf cli-results",
    "test-cli-equal-args": "./gltest --clear-config --output-dir=cli-equal-results --server-port=5151 --asset-dir=test/assets/ --coverage=true --headless=true test/test.js && test -d cli-equal-results  && test -d .nyc_output && rimraf cli-equal-results",
    "coverage": "rimraf coverage/ && ./gltest --self-test --coverage=true && nyc report --reporter=lcov"
  },
  "keywords": [
    "webgl",
    "webgl2",
    "test",
    "puppeteer"
  ],
  "author": {
    "name": "Tarek Sherif",
    "email": "tareksherif@pm.me",
    "url": "http://tareksherif.net/"
  },
  "license": "MIT",
  "dependencies": {
    "copy-dir": "^1.2.0",
    "node-glob": "^1.2.0",
    "puppeteer": "^2.0.0",
    "puppeteer-to-istanbul": "^1.2.2",
    "rimraf": "^3.0.0"
  },
  "devDependencies": {
    "coveralls": "^3.0.7",
    "eslint": "^6.6.0",
    "nyc": "^14.1.1"
  }
}
